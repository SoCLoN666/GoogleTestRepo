{
  "devDependencies": {
    "@playwright/test": "^1.30.0"
  },
  "scripts": {
    "test": "npx playwright test",
    "test-docker": "docker-compose up",
    "test-docker-chromium": "sh ./e2e/scripts/generate-docker-files.sh && docker compose -f docker-compose.chromium.yml up && sh ./e2e/scripts/cleanup.sh"
  }
}
